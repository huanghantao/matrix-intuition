"""Week 7 · 高斯消元（战场版：函数体待你实现）。"""


def solve_linear(A: list, b: list) -> list:
    """解 A x = b（A 是 n×n 方阵），返回解向量 x。

    思路：消元（把某列下面全消成 0）+ 回代（从下往上解）。
    主元为 0 时向下换行；无唯一解时抛 ValueError。
    """
    raise NotImplementedError("TODO: Week 7 Day 2 —— 高斯消元")
